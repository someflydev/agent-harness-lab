# Basic Fixture Context

This artificial `.context/` directory exists so portable helper output can
mention project-local context without using private notes.

Durable facts:

- The project is fake.
- Validation examples are offline and local.
- Future fresh sessions should read `AGENT.md` before a selected prompt.
