# Commands And Routines

This registry makes named commands and docs-only routines discoverable from one
place. A row marked as a helper-script command is executable today through
`scripts/ahl.py`. A row marked as a runbook or routine is currently manual or
assistant-executed.

| Name | Type | Purpose | Inputs | Outputs | Current implementation status | Safety notes |
| --- | --- | --- | --- | --- | --- | --- |
| `make help` / `help` | Operator console/helper-script command | List stable Makefile targets and their underlying commands. | `Makefile`, `scripts/ahl.py`, optional `--json` for direct script use. | Human list or JSON command discovery. | Implemented by `Makefile` and `scripts/ahl.py help`. | Read-only. |
| `make doctor` | Operator console target | Run the foundation doctor through the console surface. | Repo root and `.gitignore`. | Human summary from `doctor`. | Implemented in `Makefile`. | Read-only. |
| `make resume` | Operator console target | Print a grounded session context briefing through the console surface. | Git state, runtime notes, `tmp/*.md`. | Human briefing from `resume`. | Implemented in `Makefile`. | Read-only. |
| `make checkpoint` | Operator console target | Scaffold missing local context files through the console surface. | `context/*.example.md` when present. | `context/TASK.md`, `context/SESSION.md`, `context/MEMORY.md` when missing. | Implemented in `Makefile`. | Writes only missing context files; no `--force` path through Makefile. |
| `make promptset` | Operator console target | Inspect prompt numbering through the console surface. | `.prompts/PROMPT_*.txt`. | Human promptset summary. | Implemented in `Makefile`. | Read-only. |
| `make lint-prompts` | Operator console target | Run promptset prose and registry linting through the console surface. | Prompt files and `registry/prompts.json`. | Human lint summary. | Implemented in `Makefile`. | Read-only. |
| `make check-docs` | Operator console target | Run markdown navigation and local link checks through the console surface. | Docs scan roots and registries. | Human docs check summary. | Implemented in `Makefile`. | Read-only. |
| `make test` | Operator console target | Run helper CLI unit tests. | `tests/test_ahl.py`. | unittest results. | Implemented in `Makefile`. | Read-only. |
| `make trace` | Operator console target | Summarize prompt-related working tree traceability. | `PROMPT` make variable, default `PROMPT_26`. | Human trace summary. | Implemented in `Makefile`. | Read-only. |
| `make dry-run` | Operator console target | Validate all deterministic dry-run scenarios. | `dry-runs/PARITY.md`, scenario manifests, expected files. | Human dry-run check summary. | Implemented in `Makefile`. | Read-only. |
| `make registry` | Operator console target | Validate curated registry indexes through the console surface. | `registry/*.json`. | Human registry check summary. | Implemented in `Makefile`. | Read-only. |
| `make domain-pack` | Operator console target | Validate optional domain pack manifests through the console surface. | `domain-packs/*/pack.json`. | Human domain-pack check summary. | Implemented in `Makefile`. | Read-only. |
| `make lane-check` | Operator console target | Validate the committed manual lane simulation. | `simulations/lane-demo/`. | Human lane check summary. | Implemented in `Makefile`. | Read-only. |
| `make driver` | Operator console target | Validate assistant driver registry records through the console surface. | `registry/assistant-drivers.json`. | Human driver check summary. | Implemented in `Makefile`. | Read-only. |
| `make project-status` | Operator console target | Report portable target-project status through the console surface. | `PROJECT` variable, default current repo. | JSON project status. | Implemented in `Makefile`. | Read-only. |
| `make lifecycle-snippets` | Operator console target | Generate portable one-prompt lifecycle snippets. | `PROJECT` and `PORTABLE_PROMPT` variables. | JSON snippets. | Implemented in `Makefile`. | Read-only. |
| `make lifecycle-context-check` | Operator console target | Suggest portable context-update review questions. | `PROJECT` and `PORTABLE_PROMPT` variables. | JSON context-check report. | Implemented in `Makefile`. | Read-only. |
| `make lifecycle-run-range` | Operator console target | Dry-run a portable prompt range while preserving one-prompt boundaries. | `PROJECT`, `RANGE_START`, and `RANGE_END` variables. | JSON range plan. | Implemented in `Makefile`. | Read-only; writes only through direct script use with `--artifact`. |
| `make portable-fixtures` | Operator console target | Exercise portable status against artificial fixtures. | `fixtures/portable-operator/projects/basic`. | JSON fixture status. | Implemented in `Makefile`. | Read-only. |
| `make portable-rehearsal` | Operator console target | Run deterministic portable helper rehearsal. | Portable fixtures and isolated temporary git data. | Human rehearsal summary. | Implemented in `Makefile`. | Read-only for AHL; uses isolated temporary git fixture commits. |
| `make outer-plan` | Operator console target | Create an inspectable outer-loop plan. | `OUTER_FROM`, `OUTER_COUNT`, and `DRIVER` variables. | `runs/outer-loop/<plan-id>/plan.json`. | Implemented in `Makefile`. | Writes a plan artifact only. |
| `make outer-dry-run` | Operator console target | Validate an existing outer-loop plan. | `PLAN` variable. | Human dry-run summary. | Implemented in `Makefile`. | Read-only. |
| `make outer-gate` | Operator console target | Collect gate evidence for a prompt, optionally with a plan. | `PROMPT` and optional `PLAN` variables. | JSON gate report. | Implemented in `Makefile`. | Read-only; records prompt validation commands without executing arbitrary shell. |
| `make outer-run` | Operator console target | Build payloads and a ledger for an existing plan through the dry-run path. | `PLAN` variable. | Human outer-run dry-run summary and ledger artifact. | Implemented in `Makefile`. | Dry-run only; live `--execute` is direct-script only. |
| `make outer-resume` | Operator console target | Dry-run a resume point from an outer-loop ledger. | `RUN` variable. | Human resume summary. | Implemented in `Makefile`. | Read-only; refuses dirty or unsafe worktrees. |
| `make memory-check` | Operator console target | Check reviewed memory promotion candidates. | `memory/promotion-queue/*.md`. | Human memory check summary. | Implemented in `Makefile`. | Read-only. |
| `make experiment-check` | Operator console target | Check active experiment records. | `experiments/active/*`. | Human experiment check summary. | Implemented in `Makefile`. | Read-only. |
| `doctor` | Helper-script command | Check expected repo foundations and ignore rules. | Repo root, `.gitignore`, foundation paths. | Human summary or JSON checks. | Implemented in `scripts/ahl.py`. | Read-only. |
| `promptset` | Helper-script command | Inspect prompt filenames, numbering, gaps, duplicates, and strict two-digit naming. | `.prompts/PROMPT_*.txt`, optional `--json`. | Human summary or promptset JSON index. | Implemented in `scripts/ahl.py`. | Read-only; does not assess prompt prose quality. |
| `validate` | Helper-script command | Run a lightweight structural validation gate. | Repo foundations, quality directories, promptset data. | Human summary or JSON checks. | Implemented in `scripts/ahl.py`. | Read-only; does not replace prompt-specific validation. |
| `registry list` | Helper-script command | List curated registry JSON files and item counts. | `registry/*.json`, optional `--json`. | Human summary or registry-list JSON. | Implemented in `scripts/ahl.py`. | Read-only; registries are navigation aids. |
| `registry check` | Helper-script command | Validate registry JSON parsing, required item fields, local paths, and prompt ordering. | `registry/*.json`, `.prompts/PROMPT_*.txt`, optional `--json`. | Human summary or registry-check JSON. | Implemented in `scripts/ahl.py`. | Read-only; checked-in files remain source of truth. |
| `driver` | Helper-script command group | Validate driver records and run help-only probes. | `registry/assistant-drivers.json`, local `PATH`. | Driver summaries and probe diagnostics. | Implemented in `scripts/ahl.py`. | Help-only probes do not authenticate, send prompts, or prove quota. |
| `project` and `lifecycle` | Helper-script command groups | Support portable target-project status, snippets, context checks, and run-range dry-runs. | AHL checkout, target project root, prompt id or range. | Read-only status, snippets, context questions, and range plans. | Implemented in `scripts/ahl.py`. | Does not run portable prompts, edit target files, stage, commit, or continue automatically. |
| `outer` | Helper-script command group | Plan, dry-run, run, gate, status, resume, and recover bounded outer-loop prompt steps. | Prompt range, driver id, plan artifact, run ledger. | Plan, payload, ledger, gate, status, resume, and recovery artifacts. | Implemented in `scripts/ahl.py`. | Dry-run by default; live local assistant CLI invocation requires explicit `outer run --execute`. |
| `commit` | Helper-script command group | Plan prompt-scoped commits, inspect recent commits, and execute reviewed plans with explicit approval. | Prompt id, run ledger, target project git history, reviewed commit plan. | Commit plan, commit-check report, or approved commits. | Implemented in `scripts/ahl.py`. | `commit check` is read-only; `commit execute` requires `--operator-approved`. |
| `dry-run`, `lane`, `domain-pack`, `memory`, `experiment`, and `finding` | Helper-script command groups | Validate or scaffold the named local artifact family. | Scenario files, simulation workspace, domain packs, memory candidates, experiment records, finding slug. | Human or JSON checks and scaffolded artifacts. | Implemented in `scripts/ahl.py`. | Scaffolds refuse overwrite unless `--force` is explicit. |
| `resume` | Helper-script command | Print a grounded session context briefing. | Git state, `context/*.md`, `PLAN.md`, `tmp/*.md`. | Human briefing or JSON state. | Implemented in `scripts/ahl.py`. | Read-only; recommendation is advisory. |
| `checkpoint` | Helper-script command | Scaffold missing local context files from examples or minimal built-ins. | `context/*.example.md`, optional `--force`. | `context/TASK.md`, `context/SESSION.md`, `context/MEMORY.md`. | Implemented in `scripts/ahl.py`. | Writes only context files; `--force` overwrites. |
| `scaffold-run` | Helper-script command | Create a timestamped run manifest from the template. | Prompt id, assistant, permission posture, optional output dir. | `runs/<run-id>/run-manifest.md`. | Implemented in `scripts/ahl.py`. | Refuses overwrite unless `--force`. |
| `new-handoff` | Helper-script command | Create a temporary handoff scaffold. | `templates/handoffs/handoff.md`, optional `--force`. | `tmp/HANDOFF.md`. | Implemented in `scripts/ahl.py`. | Use only when bridge decision justifies it; `tmp/` is temporary. |
| `metadata-example` | Helper-script command | Print a skeleton run record for metadata-aware closeout. | Prompt id, assistant, permission posture, prompt batch id, optional `--json`. | Human summary or run-record JSON skeleton. | Implemented in `scripts/ahl.py`. | Skeleton only; repo files and git history remain authoritative. |
| Prompt Execution | Routine | Run one active prompt in a fresh session. | Operator request, `AGENT.md`, active prompt, required docs, git status. | Deliverables, validation evidence, audit and readiness notes. | Defined in `docs/routines/catalog.md` and `runbooks/fresh-session-prompt-run.md`. | One prompt per session unless operator says otherwise. |
| Completion Audit | Routine/runbook | Compare required deliverables against actual artifacts and evidence. | Active prompt, changed files, deliverables, validation notes. | Pass, warning, or blocker statement. | Defined in `docs/routines/catalog.md` and `runbooks/completion-audit.md`. | Do not claim completion for unchecked required deliverables. |
| Next-Prompt Preflight | Routine/runbook | Check whether the immediate next prompt can start cleanly. | Next prompt, current docs, navigation, residual risks. | Ready, risky, or blocked readiness report. | Defined in `docs/routines/catalog.md` and `runbooks/next-prompt-preflight.md`. | Do not implement the future prompt during preflight. |
| Bridge Decision | Routine/runbook | Decide whether a temporary handoff is worth creating. | Completion audit, readiness result, residual issues. | No-handoff decision or narrow handoff need. | Defined in `docs/routines/catalog.md` and `runbooks/bridge-fix-session.md`. | Handoff only for material blockers or warnings. |
| Handoff Generation | Routine/helper-supported | Write temporary continuation context. | Bridge reason, affected files, validation already performed. | `tmp/HANDOFF.md` when needed. | Routine plus `new-handoff` scaffold. | Temporary by default; durable claims need promotion. |
| Repair-Session Startup | Routine/runbook | Start a bounded session for blockers or small gaps. | Defect, affected files, expected fix, stop conditions. | Focused repair plan, changes, validation evidence. | Defined in `docs/routines/catalog.md` and `runbooks/repair-session.md`. | Stop when repair exceeds bounded scope. |
| Promotion Review | Routine/runbook | Decide whether transient knowledge becomes durable memory. | Candidate fact, evidence, target artifact boundary. | Promote, defer, or reject decision. | Defined in `docs/routines/catalog.md` and `runbooks/promotion-review.md`. | Raw assistant chatter is not durable source of truth. |
| Run Closeout | Routine/runbook | Leave repo ready for review or the next fresh session. | Git status, changed files, validation results, residual risks. | Final summary, readiness statement, optional handoff. | Defined in `docs/routines/catalog.md` and `runbooks/run-closeout.md`. | Do not create routine handoffs when final answer is enough. |
| Promptset Health Inspection | Routine/helper-supported | Review prompt sequence health. | Prompt files, docs indexes, completed artifacts. | Findings, warnings, suggested prompt repairs. | Routine plus `promptset` structural check. | Script checks filenames only; prose quality is human-reviewed. |
| Commit Packaging | Runbook | Group completed changes into commits when explicitly asked. | Git status, diff, prompt id, validation evidence. | Commit grouping and commit message summary. | Defined in `runbooks/commit-packaging.md`. | Never commit without operator request. |
| Assumption Diff | Routine | Compare assumptions against durable repo facts. | Assumptions, source docs, repo state. | Confirmed assumptions, conflicts, unknowns. | Defined in `docs/routines/catalog.md`. | Escalate when evidence is unavailable. |
| Scope Boundary Check | Routine | Prevent drift into future prompts or unrelated refactors. | Active prompt, constraints, artifact boundaries. | In-scope, out-of-scope, escalation notes. | Defined in `docs/routines/catalog.md`. | Protect user changes and future-prompt boundaries. |
| Validation Path Discovery | Routine | Identify cheap and relevant checks. | Changed files, README, scripts, package metadata. | Recommended commands and limits. | Defined in `docs/routines/catalog.md`. | Report skipped checks and why. |
